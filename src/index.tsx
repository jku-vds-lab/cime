import React from 'react';
import ReactDOM from 'react-dom';
import Cime from './Cime';
import './index.css'

/**
 * Main entry point for the app.
 */
ReactDOM.render(
  <React.StrictMode>
    <Cime></Cime>
  </React.StrictMode>,
  document.getElementById('root')
);
